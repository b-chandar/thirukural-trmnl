services:
  - type: web
    name: trmnl-thirukural
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: PORT
        value: 3000
      - key: OPENROUTER_API_KEY
        sync: false  # Set this manually in Render dashboard
